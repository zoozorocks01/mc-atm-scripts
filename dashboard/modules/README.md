# Dashboard Modules

Future monitor modules live here.

Planned modules:

- `power.summary`
- `power.graph`
- `inventory.summary`
- `inventory.lowStock`
- `inventory.stockPlan`
- `alerts`

Each module should declare:

- required state fields
- minimum width and height
- compact rendering mode
- full rendering mode
- whether faults inside it should pin the current page
